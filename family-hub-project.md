# Family Hub — Project Living Document

> **How to use this file**
> Keep this alongside the project. At the start of any new chat session, paste the contents so Claude has full context. Update after every working session.

---

## Key context (read this first in any new chat)

- **Project:** A wall-mounted family command center running on a tablet or touchscreen + Raspberry Pi
- **Owner:** Trey — Westminster, CO. Basic coding background (scientist-level scripting, not software dev). Has built simple apps with Claude before.
- **Family:** Trey + wife Beryl (iPhone), kids Bryce and Emery
- **Ecosystem:** Primarily Google (Gmail, Google Calendar, Google Tasks). Beryl on iPhone — must work cross-platform. Google Keep is **not used** — its API is Workspace-only and unavailable on personal Gmail accounts (confirmed 2026-08-05); groceries and any Keep-style lists live in Google Tasks instead (separate task lists, same OAuth).
- **Stack decision:** Custom React app (not Home Assistant dashboard) hosted on GitHub Pages (free). Home Assistant may be added later as a hybrid backend for smart home control — designed to be addable without breaking existing code.
- **Cost constraint:** No ongoing subscription costs. Upfront hardware/one-time costs OK.
- **Google API note:** OAuth setup is the trickiest one-time step. Once done, all subsequent Google APIs reuse the same auth.
- **Display aspect ratio:** 16:9 landscape (most common wall-mount orientation). Locked via CSS padding-top trick. Easy to change. All sizing in `em` units so it scales to any screen size.
- **AI agent:** simpsonfamilyhubapp@gmail.com, watched by a Google Apps Script (`apps-script/family-agent.gs`) that parses emails with Gemini and writes to Google Tasks/Calendar. Live and tested (T-09) — voice-dictate via Siri/Gemini to that address to add tasks, calendar events, or multi-item grocery lists hands-free.
- **App structure principle:** Modular panel/nav architecture — every feature is its own component behind a nav item. Makes adding new features (smart home, budget, etc.) trivial without touching existing code.
- **Current state:** Live React app, not the old single-HTML mockup — see "Current architecture" below for the real file layout.
- **Tooling:** Phase 3 onward should use Claude Code (CLI) for file editing + GitHub for version control and cross-machine sync.

---

## Resume on a new machine

Everything is committed and pushed to GitHub — nothing lives only on one PC. To pick this project back up anywhere:

1. **Install prerequisites** (if not already on the machine): [Node.js LTS](https://nodejs.org) and [GitHub CLI](https://cli.github.com). On Windows, `winget install OpenJS.NodeJS.LTS` and `winget install GitHub.cli` both work well via Claude Code's Bash/PowerShell tool.
2. **Clone the repo**: `git clone https://github.com/treysimpson/family-hub.git` (repo is public, so no auth needed just to clone — but `gh auth login` is needed for pushing later; walks through a one-time-code browser flow).
3. **Install deps**: `cd family-hub && npm install`.
4. **Open Claude Code in that folder** and paste this whole file into the chat (or just say "read family-hub-project.md and continue") — that's the standing instruction at the top of this doc, and it gives a fresh Claude session everything it needs.
5. **`npm run dev`** starts the app at `http://localhost:5183/family-hub/` (port is pinned in `vite.config.js` — this exact origin is authorized in the OAuth client; don't change it without also adding the new one in Google Cloud Console → APIs & Services → Credentials → "Family Hub Web Client"). Sign-in, Calendar, Tasks, Groceries, and the email agent are all live and tested (T-01–T-09 done, see To-do list below) — the remaining work is whatever Future backlog item (T-10+) gets prioritized next, or the Chores rebuild (custom localStorage-based recurrence + points system) discussed but not yet started.
6. If anything errors, check the browser console — `AppContext.jsx`'s `calendarError`/`tasksError` state and `googleAuth.js`'s `authError` are surfaced but not yet shown anywhere in the UI beyond the Settings panel's sign-in button, so console logs are the fastest way to see what broke.

**Nothing is machine-specific** — no local-only config, no secrets in untracked files, no machine-specific paths in the codebase. The OAuth client ID is public-safe and lives directly in `src/lib/googleAuth.js`. One exception: the Budget PIN lives in browser `localStorage`, not the sheet — a different browser/machine won't have it and will prompt to set a new one the first time Budget is opened there, which is expected, not a bug.

### Right now (as of 2026-08-11)

- **Outstanding manual step**: the current `apps-script/family-agent.gs` (with `processCostcoReceiptImports`, added 2026-08-10) has **not yet been pasted into script.google.com** — the Costco pipeline exists in the repo but isn't live yet. To deploy: paste the file into the Apps Script editor (see "One-time setup" note in `apps-script/README.md` — Apps Script doesn't deploy from GitHub), then add its trigger (steps 23a/23b in the same README).
- **Next up**, in the order Trey wants them:
  1. Once the script's pasted in and live, test the Costco pipeline against a real receipt — email a photo to simpsonfamilyhubapp@gmail.com, confirm it lands under the `Costco Receipt` label, then run/wait for `processCostcoReceiptImports` and check the category split looks right. This is the first real check of the Costco-specific Gemini prompt — its abbreviated item names/codes are a rougher categorization target than Target's clearer department headers, so worth a closer look than usual.
  2. Design + build **hide-surprise-transactions** (T-11's last undesigned piece, see its row below) — hiding a transaction (e.g. a Christmas gift) from the shared wall display after it's already been shown may be too late, so this likely needs to be proactive (a message-driven "hide the last transaction," similar to the fun-money pattern, or a standing list of gift-prone merchants) rather than a reactive in-app toggle. Not designed yet.

---

## Phases

| Phase | Description | Status |
|-------|-------------|--------|
| 1 — Design | Iterate on UI/UX mockups to nail look and feel | ✅ Complete |
| 2 — Navigable mockup | Interactive mockup with dummy data, all panels clickable | ✅ Complete |
| 3 — Real data | Convert to React, wire up Google APIs, deploy | ✅ Complete — repo/scaffold/deploy live, all panels ported to React, Google Calendar + Tasks APIs wired and tested against a real account, email AI agent (T-09) live and tested. Chores panel is still mock-only (a deliberate rebuild is planned, see project memory/discussion — not a Google Tasks port). |
| 4 — Smart home (optional) | Add Home Assistant hybrid for device control | Not started |

---

## Design iterations

### d.1.0 through d.1.5 — Design phase
Initial mockup through Home Control tab. Dark Dakboard-inspired theme. Settled layout, navigation structure, calendar views, weather overlay, family color coding.

### d.2.0 through d.2.5 — Navigable mockup phase

**d.2.0** — First fully navigable mockup. All placeholder panels replaced with dummy data:
- Calendar (full page with Month/Week/Agenda toggle)
- Tasks (person + time filters, scrollable list)
- Groceries (store-based: Grocery / Costco / Other)
- Chores (per-person grid with progress bars)

**d.2.1** — UI fixes batch 1: working task filters, scrollable lists, touch-friendly inputs, home screen items navigate to tabs, calendar defaults to week view, event detail side panel.

**d.2.2** — UI fixes batch 2: home calendar events navigate to Calendar tab + open event panel, weather day detail panel, filter pill contrast fixed, frequent grocery items functional, clear checked items works.

**d.2.3** — Groceries redesigned as store-based lists (Grocery / Costco / Other). Placeholder controls labeled "live in Phase 3."

**d.2.4a/b** — Two theme variants built: Hearth (warm light mode) and Dakboard (premium dark). Compared side by side.

**d.2.5** — Both themes merged into one file using CSS custom properties. Settings panel added (⚙️ in top bar). Theme choice persists via localStorage. Dark is default. Filter pill contrast fixed on dark theme.

**Known deferred items (intentional placeholders until Phase 3):**
- Calendar person filter pills toggle visually but don't hide/show events (need real data)
- Chores week navigation buttons are dimmed/disabled (need real data)

---

## Current architecture (as of 2026-08-06)

The React app lives at the repo root (not in a subfolder). Key files for a fresh session to know about:

- **`src/lib/googleAuth.js`** — Google Identity Services (GIS) browser token flow. No backend, no client secret used. Exports `signIn()`, `signOut()`, `trySilentSignIn()`, `getStoredToken()`. Token is cached in `sessionStorage` (clears on tab close, which is fine/expected).
- **`src/context/AuthContext.jsx`** — wraps the above in React state (`isSignedIn`, `accessToken`, `signIn`, `signOut`, `signingIn`, `authError`). Auto-schedules silent token refresh 5 min before expiry. Wraps the whole app in `App.jsx`, outside `AppProvider` (Auth has to exist first since `AppContext` reads from it).
- **`src/lib/googleCalendar.js`** — `fetchEvents(accessToken, timeMin, timeMax)`, normalizes raw Calendar API events into `{id, title, allDay, start, end, location, notes, who, color}`.
- **`src/lib/googleTasks.js`** — full Tasks API wrapper: `ensureTaskLists` (auto-creates named lists), `fetchTasks` (deliberately omits `showHidden` — see comment in the file), `insertTask`, `setTaskStatus`, `deleteTask`, `clearCompletedTasks`, `pruneStaleCompleted` (permanently deletes completed items older than 3 days so lists do not grow unbounded). `PERSON_LISTS` and `STORE_LISTS` constants define the 7 list names.
- **`src/lib/dateGrid.js`** + **`src/lib/buildCalendarViews.js`** — pure date-math + transform functions that turn a flat list of Calendar events into the exact month-grid/week-days/agenda-groups shapes the view components expect. No date library dependency.
- **`src/context/AppContext.jsx`** — the hub. Fetches weather (always), calendar events + tasks (only when `isSignedIn`). Tasks poll every 30s (`TASKS_REFRESH_MS`) via a ref (not the `taskListIds` state) to avoid a stale-closure bug in the interval; Tasks/Groceries pages also call `refetchTasks()` on mount so opening the tab always shows the latest data immediately. Exposes `calendarViews` (either live-built or the original mock data, picked automatically) and `tasksLive`/`personTasks`/`groceryTasks` + action functions. **Every page component branches on `tasksLive`/`calendarViews.live`** to decide whether to render real data or fall back to `src/data/mockData.js` — so the app is always fully functional and demo-able even signed out.
- **`src/lib/weather.js`** — Open-Meteo fetch + normalize, no auth needed, independent of Google sign-in.
- **`src/data/mockData.js`** — still used for family member info (names/colors), chores, home control, and as the signed-out fallback for calendar/tasks. Weather mock data was removed (T-05) since weather has no signed-out/signed-in distinction — it's just always live.
- **`apps-script/family-agent.gs`** + **`apps-script/README.md`** — the T-09 email AI agent's source, kept in the repo for version control only (Apps Script doesn't deploy from GitHub; changes must be manually re-pasted into script.google.com). Runs under Trey's own Google account, not simpsonfamilyhubapp@gmail.com, since Google Tasks lists can't be shared cross-account. Eight polling functions, each on its own 5-minute trigger except the monthly allowance one: `processFamilyAgentEmails` (T-09), `processBudgetEmails`, `processAmazonOrderEmails`, `processTargetOrderEmails`, `processTargetReceiptImports`, `processCostcoReceiptImports`, `processStatementImports` (T-11), `addMonthlyFunMoneyAllowance` (day-1-of-month trigger). Also has `lookupMerchantMemory_`, consulted by both `processBudgetEmails` and `processStatementImports` before trusting Gemini's category guess — reads the "Merchant Memory" tab, which the React app (not this script) writes to when a transaction gets manually recategorized and "Always" is chosen (T-11 Phase D). `lookupMerchantName_` runs right after (still keyed on the original raw merchant text, so a rename never affects its own category-memory entry) and reads the "Merchant Names" tab, written when a merchant gets renamed — replaces cryptic raw merchant text (e.g. "SONDERMIND INC") with a readable name (e.g. "Beryl's Therapy") on every future transaction from that source. Don't rename "Amazon.com" this way — `applyAmazonOrder_` matches on "amazon" appearing in the merchant text. **Automatic receipt routing (added 2026-08-09)**: `processFamilyAgentEmails` now checks any image attachment on a Family Agent email with `routeReceiptImage_`/`classifyReceiptImageWithGemini_` — a cheap Gemini image classification pass separate from the detailed Target-specific extraction — before trying to parse it as a task/event/grocery request. A recognized Target receipt gets moved straight to the `Target Receipt` label (no manual labeling, and it works for Beryl too, since it goes through the shared simpsonfamilyhubapp@gmail.com address rather than a self-addressed email that only Trey could send). A recognized Costco receipt is parked under a new `Costco Receipt` label, processed by `processCostcoReceiptImports` (built 2026-08-10, see below). Anything else falls through to the normal parse/Needs Review as before.
  - **Budget core** (`processBudgetEmails`) — parses Chase/Amex transaction-alert emails (labeled "Budget Agent," arriving directly in Trey's inbox, no forwarding needed) into a "Transactions" tab of an auto-created "Family Hub Budget" Google Sheet. Live and tested 2026-08-06. Categories (Trey's real household list): groceries, dining, gas-auto, travel, household, entertainment, healthcare, kids-activities, kids-other, bills-utilities, beryl-personal, trey-personal, shopping, trey-work, plus structural one-time (major/non-recurring) and other (fallback) — one-time and trey-work (reimbursable) are excluded from the regular monthly total and shown as separate lines (`EXCLUDED_FROM_BUDGET`). `setupBudgetSheets` (run manually once) creates three more tabs: **Budget Targets** (category → monthly target, populated with real categories, defaults 0), **Fixed Bills** (recurring-bill merchant names, not yet populated), **Fun Money** (see below).
  - **Fun money** (Phase C, live and tested 2026-08-06) — `fun_spend`/`fun_return` actions on `processFamilyAgentEmails` (message-driven, not card-alert-driven, so it's on the family-request pipeline not the budget one). Ledger-style: balance per person = sum of their rows, so rollover needs no extra logic. `addMonthlyFunMoneyAllowance` deposits $250 into each of Trey's and Beryl's pools on a monthly trigger — amount set in `FUN_MONEY_MONTHLY_AMOUNT`.
  - **Amazon itemization** (`processAmazonOrderEmails`, deployed and tested 2026-08-06) — Amazon's order-status emails only give item counts per coarse category, not per-item prices (no automatable/ToS-safe way to get real line-item pricing exists — Amazon has no order-history API and scraping risks account flags), so it tags the whole order with its majority category and updates the matching Transactions row in place (by amount match), never creating a new row. Handles multiple orders per email. Beryl's orders need her own sender-specific forwarding setup since they land in her inbox, not Trey's (not routed through simpsonfamilyhubapp@gmail.com, to avoid Amazon receipts getting mislabeled as family requests) — see `apps-script/README.md`. Caught and fixed a real bug during testing: `orders.every(applyAmazonOrder_)` short-circuited on the first unmatched order in a multi-order email, silently skipping every order after it (including ones that would have matched), since `.every()` stops calling its callback once it hits a false — switched to `.map()` + a separate `.every(Boolean)` check.
  - **Target order & receipt itemization** (`processTargetOrderEmails`, `processTargetReceiptImports`, Phase E, added 2026-08-07, not yet tested) — unlike Amazon, both Target.com order confirmations and the Target app's in-store Purchase History screen show real per-item prices, so these genuinely split a charge across categories instead of tagging it with one majority guess: the matching Transactions row (already created by `processBudgetEmails`) is deleted and replaced with one row per category via `splitTransactionRow_`, each holding that category's proportional share of the total (tax/shipping prorated by subtotal share via `computeCategorySplit_`), so a plain SUM still nets to the same charge. `processTargetOrderEmails` mirrors the Amazon pipeline (Gmail filter + label, matches within `TARGET_ORDER_MATCH_TOLERANCE` = $5 of amount rather than exact — found during live testing that an order confirmation total can differ from the real charge, e.g. a quoted bag fee that was not actually charged — taking whichever unclaimed Target row is closest, and splitting against that row's real charged amount rather than the email's total so the discrepancy is absorbed instead of blocking the match; same Beryl-forwarding setup). `processTargetReceiptImports` is manual, like statement import: Target does not email a receipt for in-store purchases, so a screenshot of the itemized purchase-detail screen (or a photo of a paper receipt) is emailed in and parsed with Gemini's native image understanding — same capability `processStatementImports` uses for PDFs, just a different mime type. If a card-alert row already exists for that date/amount it gets replaced with the split (`findTransactionRow_`); if not, the split is appended as new rows, filling the gap the way statement import does. **Item detail (added 2026-08-08, per Trey's request "moving forward anytime that info is available then use it")**: since Gemini already sees real item names when itemizing Target orders/receipts, `appendOrderItems_` now writes them to a new **Order Items** tab (EmailId, Item, Category) instead of throwing them away once the category subtotal is computed — keyed on EmailId rather than order number so it works uniformly for both orders and receipts (receipts have no order number). `processTargetOrderEmails` only records items for orders that actually matched on a given run (an unmatched order retries with a fresh Gemini parse next run, so recording eagerly would duplicate items once it does match). Amazon itemization and statement import never write here — neither pipeline gets real per-item names from Gemini. `appendOrderItems_` is idempotent (skips an email that already has rows), so it is safe to call more than once for the same email. `backfillTargetOrderItems`/`backfillTargetReceiptItems` (added 2026-08-09) handle orders/receipts that were already moved to `.../Done` before the Order Items tab existed — a normal re-run only looks at the still-unprocessed label and would not touch them, and by that point the Transactions row is already split so re-matching would not work anyway; these instead read the `.../Done` label directly, re-parse with Gemini, and record item names only, without touching Transactions. **Fixed 2026-08-09** (caught via live testing — Details never showed up despite Order Items having rows): `splitTransactionRow_` was carrying forward the EmailId of the *original* row it replaced, which belongs to the bank's card-alert email, not the Target order/receipt email `appendOrderItems_` actually keyed its rows on — so the two could never match. `splitTransactionRow_` now takes `emailId` as an explicit parameter (the order/receipt email's own ID) instead of reading it off the old row, threaded through from `applyTargetOrder_`/`processTargetReceiptImports`. This only fixes future runs; the two already-mismatched test rows needed a manual EmailId correction directly in the sheet.
  - **Statement import/reconciliation** (`processStatementImports`, Phase E, added 2026-08-06, not yet tested against a real statement) — Gemini has native PDF document understanding, so the statement PDF is sent directly as `inline_data` with no OCR step. Extracts every purchase/refund line item (explicitly excludes payment/balance-transfer lines, which aren't purchases), skips anything already in Transactions (matched by date + amount, since a statement's merchant text often differs from the live alert's), appends the rest as new rows tagged "Statement import" in Notes. Same function serves both the one-time historical backfill and ongoing monthly reconciliation. Manual/infrequent, so no auto-forward or filter — just email yourself the downloaded PDF with the label `Statement Import` applied. Known gaps: de-dupes on date+amount only (no stronger identifier available), and only reads the first PDF attachment per email.
  - **Manual recategorize / merchant memory / merchant rename** (Phase D, added 2026-08-06, not yet tested) — the React app writes directly to the sheet for this one (see below), not the Apps Script; `setupBudgetSheets` now creates a fourth and fifth tab, **Merchant Memory** (Merchant/Category, empty until a correction happens) and **Merchant Names** (Raw Merchant/Display Name, empty until a rename happens), which `lookupMerchantMemory_`/`lookupMerchantName_` consult before trusting Gemini in both `processBudgetEmails` and `processStatementImports`. Recategorizing now prompts "Just this once" vs. "Always for [merchant]" (only "Always" writes Merchant Memory) since merchants like Target legitimately span multiple categories and a blanket rule from one correction is often wrong; renaming always remembers (no "just this once"), since a cryptic raw merchant string is essentially always worth translating permanently.
- **`src/lib/googleSheets.js`** — Sheets API wrapper, now read/write (not read-only, as of Phase D 2026-08-06 — see `googleAuth.js` scope note below). Reads: `fetchTransactions` (each row includes its 1-indexed sheet `row` number so writes can target it directly), `fetchBudgetTargets`, `fetchFixedBills`, `fetchFunMoney`, `fetchOrderItems` (added 2026-08-08, reads the new Order Items tab; the latter four fail soft to empty rather than erroring, since those tabs may not exist yet, or a given transaction may just have no item detail at all — Amazon/statement-import rows never will). Writes: `updateTransactionCategory(accessToken, row, category)`, `updateTransactionMerchant(accessToken, row, merchant)`, `upsertMerchantMemory(accessToken, merchant, category)`, `upsertMerchantName(accessToken, merchant, displayName)` (the two upserts share a common `upsertKeyValue_` helper — updates the existing row for that key if found, else appends). Also exports `TRANSACTION_CATEGORIES`, mirrored from the Apps Script's list, for the recategorize dropdown. `BUDGET_SPREADSHEET_ID` is set to the real "Family Hub Budget" spreadsheet ID. Has a `normalizeDate` helper because Sheets silently auto-converts date-shaped strings to its own date type even when written via `appendRow`, so date cells can come back as a raw serial number instead of the "YYYY-MM-DD" string that was written — worth remembering for any future column that looks like a date.
- **`src/lib/googleAuth.js`** — `SCOPES` upgraded 2026-08-06 from `spreadsheets.readonly` to full `spreadsheets` for Phase D writes. **Needs a matching manual step**: the OAuth consent screen's declared scopes in Google Cloud Console still need the non-readonly scope added (Data Access → Add or remove scopes → Google Sheets API → `.../auth/spreadsheets`), then sign out/in again in the app — same kind of step as the original `spreadsheets.readonly` addition. Not yet done as of this writing.
- **`src/context/AppContext.jsx`** — `selectedBudgetMonth`/`setSelectedBudgetMonth` (added 2026-08-07, a "YYYY-MM" string, defaults to the real current calendar month) now drives every Budget total (`budgetMonthTotal`, `budgetCategoryTotals`, `budgetOneTimeTotal`, `budgetReimbursableTotal`, `budgetFixedTotal`, `budgetDiscretionaryTotal`, and the raw `selectedMonthTransactions` list) — these were already calendar-month-boundary math (`t.date.startsWith(monthPrefix)`, not a rolling N-day window), but were previously locked to whatever `new Date()` returned with no way to look at a past month; making the month an explicit, navigable piece of state is what actually fixes that. Fun Money intentionally is NOT scoped to this — it stays its own ledger-style running total regardless of which month is selected. `recategorizeTransaction(transaction, category, remember = true)` (added 2026-08-06): optimistically updates local state, then calls `updateTransactionCategory`; rolls the optimistic update back and sets `budgetActionError` if that write fails (self-reviewed catch before this was ever live-tested — an unrolled-back optimistic update would have shown a category as saved when it was not, e.g. during tonight's own window before the OAuth scope is upgraded). `upsertMerchantMemory` is separately best-effort (logged, not surfaced) since the category change is the part that matters to the user, and is skipped entirely when `remember` is false (the "just this once" choice). `renameMerchant(transaction, displayName)` (fixed 2026-08-08 — originally only renamed the single tapped row) now finds every existing transaction sharing the same raw merchant text and renames all of them, not just the one tapped: optimistically updates all matching rows, writes each via `updateTransactionMerchant` with `Promise.allSettled`, and rolls back only the rows whose write actually failed (not the whole batch) before best-effort `upsertMerchantName` for future transactions.
- **`src/components/PinGate.jsx`** — client-side-only PIN gate (localStorage, not real security — good enough to stop casual viewing on the shared wall display, not a determined attacker) used by `BudgetPage.jsx`. Reset via Settings panel ("Forgot PIN").
- **`src/pages/BudgetPage.jsx`** — a `‹ Month Year ›` nav row (added 2026-08-07, next-month arrow hidden once you're back at the real current month — can't navigate into the future) sits above the total-spend card and drives `selectedBudgetMonth`, so every sidebar stat and the whole transactions list below reflect whichever month is selected, not just "now": total monthly spend across all cards (tap to drill into a per-category breakdown, with over/under shown once a category has a non-zero target), a "Major expenses this month" line for the one-time bucket, a "Trey's work (pending reimbursement)" line, a fixed-vs-discretionary split (once Fixed Bills has entries), Fun Money balance cards for Trey/Beryl (not scoped to the selected month — ledger-style running total — red/coral styling when a balance drops below `FUN_MONEY_LOW_THRESHOLD`, currently a flat $25) with a recent-spends list, and a "{Month Year} transactions" list showing *every* transaction in the selected month (not a fixed recent-10 — this is the "click a month, see all its details" view) where tapping a transaction's category opens an inline dropdown, then a "Just this once" / "Always for [merchant]" choice before actually saving (added 2026-08-06), and tapping the merchant name itself turns it into a text box to rename it (also added 2026-08-06) — both blocked on the OAuth scope step above until they can actually write. A row also shows a "Details" toggle (added 2026-08-08, via `orderItemsByEmailId` from AppContext; **filtered to that row's own category as of 2026-08-09** — a source email's item list spans every category the order got split into, so without filtering the Household row would also show the Kids Other items) whenever it has item-level data for its own category — expands to just that row's item names, no prices shown per Trey's request. Only Target order/receipt transactions ever have this; Amazon and statement-import rows never show a Details control since neither pipeline captures real item names — all gated by `PinGate`. Sign-in check uses `useAuth()` directly (not `useApp()`, which doesn't re-expose `isSignedIn` — caused a real bug where the page always showed "not signed in" regardless of actual state, fixed 2026-08-06).

**Design pattern to preserve going forward:** any new Google-backed feature should follow the same shape — a thin `src/lib/googleX.js` REST wrapper, state + derived view in `AppContext`, and the consuming page component branching on a `live` flag with a mock fallback. This keeps the app safely demoable at every commit and matches how Calendar/Tasks were done.

---

## Phase 3 — Build plan (up next)

**Recommended order:**

| Step | Task | Notes |
|------|------|-------|
| 3a | Set up GitHub repo | Enables cross-machine sync + is required for GitHub Pages deploy |
| 3b | Convert HTML mockup to React app | Use Claude Code. Scaffold with Vite + React. Each panel becomes a component. |
| 3c | Deploy skeleton to GitHub Pages | Get a real URL before touching OAuth |
| 3d | Set up Google Cloud project + OAuth | One-time. Unlocks all Google APIs. Requires a real URL. |
| 3e | Wire up weather API | Easiest first win — no OAuth needed. Use Open-Meteo (free, no key). |
| 3f | Wire up Google Calendar | OAuth required. Month + week + agenda views. |
| 3g | Wire up Google Tasks | Reuse same OAuth. Today / this week / by date filters. |
| 3h | Wire up Google Tasks for groceries | Same OAuth. Store-based lists via task list names. |
| 3i | Set up AI agent | simpsonfamilyhubapp@gmail.com + Google Apps Script. Watches inbox, parses, writes to Google services. |

---

## To-do list

### Pending

| # | Task | Phase | Notes |
|---|------|-------|-------|
| T-10 | (Future) Add Home Assistant for smart home | Phase 4 | Pi backend, React calls HA local REST API |
| T-11 | Budget tab | Phase 3+ | 🔄 In progress — **Phase A complete and tested** (total monthly spend, PIN lock, real Chase transaction confirmed end-to-end 2026-08-06). **Phase B complete and tested** (real 14-category list, category drill-down, one-time/major-expense bucket, trey-work reimbursement bucket, budget-targets populated with real categories, Amazon order itemization deployed and tested — fixed-bills infrastructure exists but not yet populated with real merchant names, that's on Trey whenever convenient). **Phase C (fun-money tracker) built and live**: `fun_spend`/`fun_return` actions, $250/mo each for Trey and Beryl via `addMonthlyFunMoneyAllowance`, ledger-based balances with automatic rollover, low-balance UI warning. **Phase D (manual recategorize + merchant memory + merchant rename) built 2026-08-06/07, tested and confirmed working 2026-08-08** — recategorize asks "just this once" vs. "always" before writing a merchant-memory rule (Target-style mixed-category merchants no longer get force-categorized from one correction), and merchants can be renamed (e.g. "SONDERMIND INC" → "Beryl's Therapy") — a rename fix on 2026-08-08 made it apply to every existing transaction sharing that raw merchant text, not just the one tapped (originally only renamed the single row); see "Current architecture" below. **Phase E: statement import/reconciliation built 2026-08-06, tested 2026-08-09 against a real statement — worked fine** (`processStatementImports`); **Target order & receipt itemization built 2026-08-07, tested and confirmed working end-to-end 2026-08-09** (`processTargetOrderEmails` for Target.com orders, `processTargetReceiptImports` for in-store Circle-scanned purchases, `routeReceiptImage_` auto-classifying and routing a receipt screenshot sent to simpsonfamilyhubapp@gmail.com — no manual labeling needed, works for Beryl too) — genuinely splits a charge across categories using real per-item pricing (unlike Amazon's majority-tag approach), plus a per-transaction item "Details" view (filtered to that row's own category) confirmed working; see "Current architecture" below. Along the way, live testing caught and fixed three real bugs: an order-total-vs-real-charge tolerance issue (a quoted bag fee that was not actually charged), an EmailId join-key mismatch that silently broke the Details lookup, and the merchant-rename-only-affects-one-row issue above. **Costco receipt import built 2026-08-10** (`processCostcoReceiptImports`, mirrors Target receipt import's logic exactly — same replace-existing-row-or-append-new behavior, same category-split math — with a Costco-specific Gemini prompt tuned for its terse abbreviated item names instead of Target's department-labeled purchase history; see `apps-script/README.md` for setup). **Month-over-month trend view built 2026-08-10** (Nivo-based: a money-flow Sankey, category pie, and total-spend trend line for the selected month, plus per-category 6-month bar charts — tapping a category also filters the transaction list down to just it). **"How to add transactions" reference button + missing-receipt flag built 2026-08-10** (T-23, see its row below) — a `?`-style button next to the Budget title shows a quick-reference list of every input method, plus a live "Receipts not yet sent in" list of Target/Costco/Amazon transactions that are still just the card alert's plain guess (heuristic: no itemization tag in Notes), with a count badge on the button itself so the gap is visible without opening the panel. hide-surprise-transactions remains unbuilt. Separately (2026-08-07): the Budget page's month is now an explicit, navigable calendar-month selection (`‹ Month Year ›`) rather than always implicitly "whatever `new Date()` is right now" — the underlying math was already calendar-month boundaries, not a rolling window, but there was no way to look at a past month; the transactions list now shows every transaction in the selected month, not a fixed recent-10. Fun Money deliberately stays out of this — it is its own ledger-style running total. See "Current architecture" below for exact file/function names. **Design constraint: keep the sheet schema clean and portable** (plain columns, standard date formats, no crammed multi-value cells) — Trey wants this data usable outside the Hub app too (other apps, manual spreadsheet formulas), not locked into an app-only format. Build order: **A)** Sheets backing store + total monthly spend across all cards + PIN lock (T-12) on the section — **B)** per-transaction categorization + Amazon order itemization (**revised 2026-08-06**: real Amazon order-status emails only give item counts per coarse category, not per-item prices, and there's no automatable/ToS-safe way to get true line-item pricing — Amazon has no public order-history API, and scraping the order-details page risks the account getting flagged. So instead of splitting a mixed order's dollar amount, the whole order gets tagged with its majority item category, matched to its card charge by amount, updating that Transactions row in place) + category drill-down + category budget targets (over/under) + one-off/major-expense bucket (e.g. HVAC replacement, excluded from regular budget) + recurring/fixed-bill tracking — **C)** personal "fun money" allowance tracker for Trey + Beryl (agent-driven via message, e.g. "Beryl spent $120 on shoes", with month-end rollover) + low-balance nudge, largely independent of Phase B so it can run in parallel — **D)** manual in-app recategorize + merchant memory (a correction becomes a standing rule, cutting future LLM cost/errors) — **E)** receipt-based category splitting for mixed-category stores like Target/Costco (needs image input + OCR/vision parsing, most complex item, deliberately last) + month-over-month trend view (needs a couple months of real data to be useful anyway) + retroactive backfill of old statement data with categories applied (Trey has old statements available for this) + monthly statement upload as an ongoing reconciliation check (same statement-parsing capability as the backfill, applied recurring rather than once — cross-checks that every transaction alert was actually captured and lets Trey fix any gaps/errors) + hide/surprise transactions (e.g. Christmas gifts) so they don't show on the shared wall display — needs design: toggling "hide" after a transaction's already been shown may be too late, so likely needs to be proactive (e.g. a message-driven "hide the last transaction" similar to the fun-money pattern, or a standing list of gift-prone merchants) rather than a reactive in-app toggle. |
| T-12 | Pin code lock for tabs | Phase 3+ | Scoped to the Budget tab first (part of T-11 Phase A), not all tabs — general purpose (e.g. locking other tabs) is a possible later extension. |
| T-13 | (Future) Sports scores & schedules | Phase 3+ | **Feasible.** ESPN undocumented API or free SportsDataIO tier. No auth for read-only scores. |
| T-14 | (Future) Solar generation — Enphase/Enlighten | Phase 3+ | **Feasible.** Free API for system owners. Requires Enlighten developer account + API key. |
| T-15 | (Future) Eufy doorbell/camera live feed | Phase 3+ | **Difficult.** No official API. Best path: Home Assistant Eufy integration as proxy → HLS stream. Requires Phase 4 first. |
| T-16 | (Future) Remote start — MySubaru | Phase 3+ | **Difficult/risky.** No official API. Community workarounds have been blocked by Subaru before. Not recommended yet. |
| T-17 | (Future) Paprika → grocery list | Phase 3+ | **Feasible with workaround.** No public API but supports Dropbox export. Script reads export → appends to Google Tasks. |
| T-18 | (Future) Dinner menu tab | Phase 3+ | **Feasible, design as standalone app first.** Pull from Paprika via Dropbox, show recipe cards, add to meal plan + grocery list. |
| T-19 | (Future) Google Photos screensaver | Phase 3+ | **Feasible.** On inactivity, switch to fullscreen slideshow with weather + time overlay. Reuses Google OAuth already planned. |
| T-20 | (Future) Spotify now playing / control | Phase 3+ | **Feasible.** Spotify Web API is free + well-documented. Show track, album art, play/pause/skip. Spotify Connect for speaker control. |
| T-21 | (Future) Signal-based agent messaging | Phase 4 | **Feasible, needs Phase 4 Pi first.** Signal has no official bot API — the unofficial `signal-cli` (or `signal-cli-rest-api` Docker wrapper) can send/receive, but needs a dedicated Signal identity (separate number or linked device) and an always-on process, which Google Apps Script can't provide. Natural fit once the Phase 4 Pi exists for smart-home control; not viable standalone. |
| T-22 | (Future) In-app / email bug reporting | Phase 3+ | Idea from 2026-08-08: let Beryl (or anyone using the wall display) report a bug without needing Trey there — either a lightweight "report a bug" control in the app itself, or an email to the existing agent address reusing the same Gemini/label pipeline pattern already built for T-09/T-11 (e.g. a "Bug Report" label + a `processBugReportEmails` function). Either path needs somewhere to land: could append to a new "Bug Reports" tab in a sheet, or append directly into this project doc's to-do list. Not designed yet — needs a decision on where reports get stored/surfaced so they actually get looked at later rather than silently landing somewhere unread. |
| T-24 | (Future) Gym/rec center class schedule → calendar | Phase 3+ | Idea from 2026-08-10: email (or upload) a PDF of a rec center's class schedule to the agent, tell it which classes to add (e.g. "add Bryce's swim lessons and the family yoga class"), and it finds all the recurring times in the PDF and adds them to a gym class calendar — same Gemini native-PDF-understanding capability already used for statement import, applied to a recurring weekly schedule instead of a list of transactions. Not designed yet — open questions: how the user specifies which classes to pick out of a full schedule (a follow-up email? a checklist?), whether recurring weekly events need their own Google Calendar (separate from the single shared calendar the family agent currently writes everything to) so they don't clutter the main view, and how to handle a schedule that changes session to session (re-running it each season, replacing old entries vs. leaving stale ones). |
| T-25 | (Future, optional) Trey-work reimbursement tracking | Phase 3+ | Idea from 2026-08-10, deferred — Trey already tracks reimbursement status himself outside the app. Would be a small ledger (mark a trey-work transaction paid/pending) similar in spirit to the Fun Money ledger. Not the same as the "view trey-work transactions" list, which is being built now as part of the Budget suite batch (T-11) — this is specifically about tracking whether the money actually came back. |
| T-26 | (Future, optional) Sinking funds / savings goals | Phase 3+ | Idea from 2026-08-10, deferred — Trey already handles this manually by setting target spend below income. Would be a ledger for planned big-ticket expenses (property tax, holiday gifts, next car), similar pattern to Fun Money, so a known upcoming expense can be pre-funded instead of landing as a surprise "one-time" hit. Ties in with the still-undesigned hide-surprise-transactions idea under T-11. |

### Done

| # | Task | Completed | Notes |
|---|------|-----------|-------|
| — | Brainstorm project architecture | 2026-03-20 | Settled on React + Google APIs + GApps Script agent |
| — | Choose Home Assistant vs React app | 2026-03-20 | React app chosen; HA deferred to Phase 4 |
| — | Design phase (d.1.0 → d.1.5) | 2026-03-20 | Layout, nav, calendar, weather, home control, family names |
| — | Navigable mockup (d.2.0 → d.2.5) | 2026-05-16 | All panels functional, dual theme, settings panel, syntax errors fixed |
| T-01 | Set up GitHub repo | 2026-08-05 | Public repo at github.com/treysimpson/family-hub (public needed for free GitHub Pages) |
| T-02 | Scaffold Vite + React app | 2026-08-05 | Vite/React scaffold merged into repo root, base path set to `/family-hub/`. Panel conversion from mockup tracked separately as T-02b. |
| T-03 | Deploy skeleton to GitHub Pages | 2026-08-05 | Live at https://treysimpson.github.io/family-hub/ via GitHub Actions (deploys automatically on push to master) |
| T-02b | Convert each mockup panel to a React component | 2026-08-05 | All 7 panels ported (Home, Calendar, Tasks, Groceries, Chores, Home Control, Weather) with working filters/toggles/add-item state, event + weather-day detail panels, settings panel with persisted theme. Consolidated the mockup's duplicated month/week/agenda dummy data into one shared model (src/data/mockData.js) with reusable MonthView/WeekView/AgendaView components, so real Google Calendar data (T-06) can slot in without restructuring. Fixed a layout bug carried over from the mockup: `.aspect-wrap` had no `overflow:hidden`, so closed side panels (hidden via `transform: translateX(100%)`) were visible outside the screen bounds. Verified visually in both themes via Claude in Chrome. |
| T-04 | Set up Google Cloud project + OAuth | 2026-08-05 | GCP project "Family Hub" (ID `family-hub-504703`), Calendar API + Tasks API enabled. OAuth consent screen: External audience, Testing publishing status (no verification needed for a 2-person app), scopes `auth/calendar` + `auth/tasks`, test users wdsimpson3@gmail.com + beryl131@gmail.com. Web OAuth client "Family Hub Web Client" created with authorized JS origins `https://treysimpson.github.io` and `http://localhost:5183` (no redirect URI — app will use Google Identity Services' browser token flow, no backend). Client ID: `214844293769-vrmfijk6r20969u86vkb4706e4p7gkrd.apps.googleusercontent.com` (safe to reference in frontend code/config — not secret for public clients). Client secret was shown once but is unused and wasn't saved anywhere. |
| T-05 | Wire up weather (Open-Meteo) | 2026-08-05 | `src/lib/weather.js` fetches + normalizes live Open-Meteo data for Westminster, CO (no API key). Shared via AppContext (one fetch, refreshed every 15 min) — powers both the topbar's inline temp and the full Weather page, including day-detail panel. WMO weather codes mapped to the app's emoji icon set, with day/night icon variants. Removed now-dead dummy weather exports from mockData.js. |
| T-06 | Wire up Google Calendar API | 2026-08-06 | Google Identity Services browser token flow (`src/lib/googleAuth.js` + `AuthContext.jsx`) — no backend, no client secret, sign-in/out lives in the Settings panel. Real events (`googleCalendar.js`) get transformed (`buildCalendarViews.js` + `dateGrid.js`) into the same month/week/agenda/next-5-days shapes the UI already expects, computed from the actual current date. Home + Calendar page branch on `calendarViews.live`: real data + real per-day weather when signed in, original mock data when signed out. Person filter pills on the Calendar page stay visual-only — a single primary calendar has no per-family-member signal to filter by (would need separate per-person calendars merged together, a possible future item). **Not yet tested against a real account** — see "Resume on a new machine" above. |
| T-07, T-08 | Wire up Google Tasks API (tasks + groceries) | 2026-08-06 | `src/lib/googleTasks.js` — full REST wrapper. Both Tasks and Groceries need per-category data Google Tasks doesn't support as custom fields, so each gets its own auto-created named list (Trey/Beryl/Kids/Family for tasks; Grocery/Costco/Other for groceries) via `ensureTaskLists` on first sign-in — these lists also show up in Google Tasks on Trey/Beryl's phones. AppContext flattens all 7 lists into `personTasks`/`groceryTasks` with optimistic-update actions (toggle/add/delete/clear-completed). TasksPage, GroceriesPage, and Home's grocery teaser branch on `tasksLive`. Tested against a real account 2026-08-06 — round-trip sync, due dates, "Clear checked items" on both Tasks and Groceries, frequent-item chips (state derived from the live list rather than tracked separately, so it can't get stuck), and Home's Family panel (now grouped by the 4 real Tasks lists rather than by individual person, since Bryce/Emery share one "Kids" list) all confirmed working. |
| T-09 | Set up simpsonfamilyhubapp@gmail.com AI agent | 2026-08-06 | simpsonfamilyhubapp@gmail.com account created, forwards to wdsimpson3@gmail.com, filtered there with a "Family Agent" Gmail label. Gemini API key from AI Studio (tied to `family-hub-504703`). Apps Script source at `apps-script/family-agent.gs` (see `apps-script/README.md` for deploy steps) — runs under Trey's own account, not simpsonfamilyhubapp@gmail.com, since Google Tasks lists can't be shared cross-account; simpsonfamilyhubapp@gmail.com is just the intake address. Gemini 3.6 Flash parses each email into a JSON array of actions (add_task/add_event/add_grocery), one entry per distinct request, so multi-item emails (e.g. a grocery list) split into separate entries instead of one combined title. Deployed and end-to-end tested: voice-dictated emails (Siri/Gemini) correctly create tasks (with due dates, correct person list), calendar events (with relative-date resolution and person-name-prefixed titles), and multi-item grocery lists. |
| T-23 | In-app "how to" / help button | 2026-08-10 | Idea from 2026-08-09, built 2026-08-10 — a button next to the Budget title shows a quick-reference list of how each input method works (Target online — automatic, Target/Costco in-store — email a photo, Amazon — automatic majority-tag, statement import — email a PDF, fun money — message the agent), plus a live "receipts not yet sent in" list (Target/Costco/Amazon transactions with no itemization tag yet) with a count badge on the button itself. `src/components/panels/HowToPanel.jsx`. |

---

## Things to never forget

- **Module/panel architecture from day 1** — every feature is its own React component behind a nav item. Never build monolithic pages.
- **Google Home API is restricted** — no public API for device control. Must go through Home Assistant (Phase 4). Don't try to call Google Home directly from React.
- **Beryl is on iPhone** — any sync mechanism must work natively on iOS. Google Tasks + Calendar both have good iOS apps. Avoid Android-only solutions.
- **GitHub Pages is the deploy target** — static hosting, free. React app must be a static build (no Node server at runtime). Vite handles this cleanly.
- **Google Apps Script agent is serverless** — runs on Google's infrastructure. Pi not required for the agent.
- **Google Keep has no API for personal accounts** — Keep API is Workspace-only (admin-approved business accounts). Dropped from the plan 2026-08-05; everything Keep would've done lives in Google Tasks instead.
- **One Google OAuth setup covers Calendar + Tasks** — same credentials, no Keep scope needed.
- **OAuth requires a real URL** — set up GitHub Pages deploy before configuring Google Cloud OAuth consent screen.
- **OAuth app stays in "Testing" mode** — External audience, only the 2 test users (Trey + Beryl) can authorize it; no Google app verification needed since it never goes to Production. If either sign-in ever fails with an "app not verified" or "access blocked" error, check that their email is still listed under Audience → Test users in the Google Auth Platform console.
- **Beryl must complete her own Google sign-in/consent** the first time she uses the app on her device — Trey authorizing doesn't cover her account.
- **Use Claude Code for Phase 3** — much better than chat for file editing, running commands, and iterating on a real codebase. Use GitHub to sync across machines.
- **Dark theme contrast** — defer readability tuning until app is on the actual wall display. Hardware + ambient light affects this significantly.
- **Open-Meteo for weather** — free, no API key, no auth. Just lat/long. Perfect for this use case.
